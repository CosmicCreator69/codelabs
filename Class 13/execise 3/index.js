var Person = /** @class */ (function () {
    function Person() {
        this.favoriteColor = "";
        this.LogFavoriteColor = console.log(person.favoriteColor);
    }
    return Person;
}());
var person = new Person();
{
    person.favoriteColor = "blue";
}
